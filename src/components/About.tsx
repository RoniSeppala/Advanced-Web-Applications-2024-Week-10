import React from "react";

interface aboutProps {
    
}

const About:React.FC<aboutProps> = () => {
  return (
    <div>
        This is some random text inside the Header component. 1
    </div>
  );
};

export default About;